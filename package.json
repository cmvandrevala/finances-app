{
  "scripts": {
    "setup": "yarn install && elm-package install",
    "test": "elm test",
    "start": "rm -rf public && brunch build && brunch watch --server"
  },
  "devDependencies": {
    "elm": "0.18",
    "elm-brunch": "^0.11.1",
    "brunch": "^2.10.17",
    "elm-test": "^0.18.13-beta3"
  }
}
