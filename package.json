{
  "scripts": {
    "setup": "yarn install && elm-package install",
    "test": "elm test",
    "start": "rm -rf public && brunch build && brunch watch --server"
  },
  "devDependencies": {
    "brunch": "^2.10.12",
    "elm-brunch": "^0.11.1"
  }
}
