{
  "scripts": {
    "setup": "yarn install && elm-package install",
    "test": "elm test",
    "build": "rm -rf public && brunch build && brunch watch --server"
  },
  "devDependencies": {
    "elm-brunch": "^0.11.1"
  }
}
